import React from "react";
import {z} from "zod";
import { zodResolver } from "@hookform/resolvers/zod";

export const TableBuilder = () => {
    return (
        <div>
            <table>
                <thead>
                    <tr></tr>
                </thead>
            </table>
        </div>
    )
}